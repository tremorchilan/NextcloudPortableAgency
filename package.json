{
  "name": "empire-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Empire Engine — Sovereign Folder-Centric Orchestration. Meta-Harness VS Code extension, OneCLI secret guardian, and Empire folder templates.",
  "workspaces": [
    "extension",
    "onecli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run build && npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
