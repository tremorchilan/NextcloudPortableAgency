# Website

Public-facing site for {{EMPIRE_NAME}}.

- `generated/` — code produced by OpenDesign from `design/` (regenerated
  automatically; edit `design/` instead).
- Everything else is yours. Agents write here during sessions.
