# OpenFang (optional AI worker)

Optional second AI worker for {{EMPIRE_NAME}}. Same conventions as Hermes —
agents create the config during a session if needed. Credentials are always
placeholders resolved by OneCLI.
