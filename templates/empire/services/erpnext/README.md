# ERPNext

Business resource planning for {{EMPIRE_NAME}} (invoicing, inventory, …).

- Install via Dockge during setup. ERPNext is heavy — give it a moment.
- Agents integrate via the Frappe REST API; credentials stay as placeholders.
