# Workflows

n8n workflow exports for {{EMPIRE_NAME}}. Agents create workflows via the n8n
API and export them here so they survive re-installs and exports.

| File | What it does |
|------|--------------|
| (none yet) | Create your first workflow in n8n and export it here |

Convention: one JSON export per workflow, named after its purpose
(`onboarding.json`, `invoicing.json`, …).
